name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Vue build output
        uses: actions/cache@v4
        id: build-cache
        with:
          path: completa
          key: vue-build-${{ hashFiles('inventario-vue/src/**', 'inventario-vue/package-lock.json', 'inventario-vue/vite.config.js', 'inventario-vue/package.json') }}

      - name: Setup Node.js
        if: steps.build-cache.outputs.cache-hit != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: inventario-vue/package-lock.json

      - name: Install dependencies
        if: steps.build-cache.outputs.cache-hit != 'true'
        working-directory: inventario-vue
        run: npm ci

      - name: Build Vue app
        if: steps.build-cache.outputs.cache-hit != 'true'
        working-directory: inventario-vue
        run: npm run build

      - name: Prepare site
        run: |
          mkdir -p _site
          cp index.html _site/
          cp -r assets _site/
          cp -r light _site/
          cp -r completa _site/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
